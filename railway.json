{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn wsgi:app --bind 0.0.0.0:${PORT:-8080} --timeout 120 --workers 2",
    "restartPolicyType": "ON_FAILURE"
  }
}
